export const chartData = (state, chartData) => {
  state.chartData = chartData;
};
export const chartOptions = (state, chartOptions) => {
  state.chartOptions = chartOptions;
};
export const selectedOption = (state, selectedOption) => {
  state.selectedOption = selectedOption;
};
